---
Write a JavaScript function that returns in an array the results of the multiplication table of a number from 1 to 10 using a "while" loop.
---

```js
function multiplicationTable(n) {
  let result = [];
  let i = 1;
  while (i <= 10) {
    result.push(n * i);
    i++;
  }
  return result;
}
```

```js
function multiplicationTable(n) {
  let result = [];
  let i = 1;
  while (i < 10 + 1) {
    result.push(n * i);
    i++;
  }
  return result;
}
```

```js
function multiplicationTable(n) {
  let result = [];
  let i = 1;
  while (i < 11) {
    result.push(n * i);
    i++;
  }
  return result;
}
```

```js
function multiplicationTable(n) {
  let result = [];
  let i = 0;
  while (i < 10) {
    result.push(n * (i + 1));
    i++;
  }
  return result;
}
```

```js
function multiplicationTable(n) {
  let result = [];
  let i = 1;
  while (true) {
    result.push(n * i);
    i++;
    if (i > 10) {
      break;
    }
  }
  return result;
}
```

```js
function multiplicationTable(n) {
  let result = [];
  let i = 1;
  while (true) {
    result.push(n * i);
    if (i >= 10) {
      break;
    }
    i++;
  }
  return result;
}
```

```js
function multiplicationTable(n) {
  let i = 1;
  let result = [];
  while (i <= 10) {
    result.push(n * i);
    i++;
  }
  return result;
}
```

```js
function multiplicationTable(n) {
  let i = 1;
  let result = [];
  while (i < 10 + 1) {
    result.push(n * i);
    i++;
  }
  return result;
}
```

```js
function multiplicationTable(n) {
  let i = 1;
  let result = [];
  while (i < 11) {
    result.push(n * i);
    i++;
  }
  return result;
}
```

```js
function multiplicationTable(n) {
  let i = 0;
  let result = [];
  while (i < 10) {
    result.push(n * (i + 1));
    i++;
  }
  return result;
}
```

```js
function multiplicationTable(n) {
  let i = 1;
  let result = [];
  while (true) {
    result.push(n * i);
    i++;
    if (i > 10) {
      break;
    }
  }
  return result;
}
```

```js
function multiplicationTable(n) {
  let i = 1;
  let result = [];
  while (true) {
    result.push(n * i);
    if (i >= 10) {
      break;
    }
    i++;
  }
  return result;
}
```

```js
function multiplicationTable(n) {
  const result = [];
  let i = 1;
  while (i <= 10) {
    result.push(n * i);
    i++;
  }
  return result;
}
```

```js
function multiplicationTable(n) {
  const result = [];
  let i = 1;
  while (i < 10 + 1) {
    result.push(n * i);
    i++;
  }
  return result;
}
```

```js
function multiplicationTable(n) {
  const result = [];
  let i = 1;
  while (i < 11) {
    result.push(n * i);
    i++;
  }
  return result;
}
```

```js
function multiplicationTable(n) {
  const result = [];
  let i = 0;
  while (i < 10) {
    result.push(n * (i + 1));
    i++;
  }
  return result;
}
```

```js
function multiplicationTable(n) {
  const result = [];
  let i = 1;
  while (true) {
    result.push(n * i);
    i++;
    if (i > 10) {
      break;
    }
  }
  return result;
}
```

```js
function multiplicationTable(n) {
  const result = [];
  let i = 1;
  while (true) {
    result.push(n * i);
    if (i >= 10) {
      break;
    }
    i++;
  }
  return result;
}
```

```js
function multiplicationTable(n) {
  let i = 1;
  const result = [];
  while (i <= 10) {
    result.push(n * i);
    i++;
  }
  return result;
}
```

```js
function multiplicationTable(n) {
  let i = 1;
  const result = [];
  while (i < 10 + 1) {
    result.push(n * i);
    i++;
  }
  return result;
}
```

```js
function multiplicationTable(n) {
  let i = 1;
  const result = [];
  while (i < 11) {
    result.push(n * i);
    i++;
  }
  return result;
}
```

```js
function multiplicationTable(n) {
  let i = 0;
  const result = [];
  while (i < 10) {
    result.push(n * (i + 1));
    i++;
  }
  return result;
}
```

```js
function multiplicationTable(n) {
  let i = 1;
  const result = [];
  while (true) {
    result.push(n * i);
    i++;
    if (i > 10) {
      break;
    }
  }
  return result;
}
```

```js
function multiplicationTable(n) {
  let i = 1;
  const result = [];
  while (true) {
    result.push(n * i);
    if (i >= 10) {
      break;
    }
    i++;
  }
  return result;
}
```

```js
function multiplicationTable(number) {
  let table = [];
  let index = 1;
  while (index <= 10) {
    table.push(number * index);
    index++;
  }
  return table;
}
```

```js
function multiplicationTable(number) {
  let table = [];
  let index = 1;
  while (index < 10 + 1) {
    table.push(number * index);
    index++;
  }
  return table;
}
```

```js
function multiplicationTable(number) {
  let table = [];
  let index = 1;
  while (index < 11) {
    table.push(number * index);
    index++;
  }
  return table;
}
```

```js
function multiplicationTable(number) {
  let table = [];
  let index = 0;
  while (index < 10) {
    table.push(number * (index + 1));
    index++;
  }
  return table;
}
```

```js
function multiplicationTable(number) {
  let table = [];
  let index = 1;
  while (true) {
    table.push(number * index);
    index++;
    if (index > 10) {
      break;
    }
  }
  return table;
}
```

```js
function multiplicationTable(number) {
  let table = [];
  let index = 1;
  while (true) {
    table.push(number * index);
    if (index >= 10) {
      break;
    }
    index++;
  }
  return table;
}
```

```js
function multiplicationTable(number) {
  let index = 1;
  let table = [];
  while (index <= 10) {
    table.push(number * index);
    index++;
  }
  return table;
}
```

```js
function multiplicationTable(number) {
  let index = 1;
  let table = [];
  while (index < 10 + 1) {
    table.push(number * index);
    index++;
  }
  return table;
}
```

```js
function multiplicationTable(number) {
  let index = 1;
  let table = [];
  while (index < 11) {
    table.push(number * index);
    index++;
  }
  return table;
}
```

```js
function multiplicationTable(number) {
  let index = 0;
  let table = [];
  while (index < 10) {
    table.push(number * (index + 1));
    index++;
  }
  return table;
}
```

```js
function multiplicationTable(number) {
  let index = 1;
  let table = [];
  while (true) {
    table.push(number * index);
    index++;
    if (index > 10) {
      break;
    }
  }
  return table;
}
```

```js
function multiplicationTable(number) {
  let index = 1;
  let table = [];
  while (true) {
    table.push(number * index);
    if (index >= 10) {
      break;
    }
    index++;
  }
  return table;
}
```

```js
function multiplicationTable(number) {
  const table = [];
  let index = 1;
  while (index <= 10) {
    table.push(number * index);
    index++;
  }
  return table;
}
```

```js
function multiplicationTable(number) {
  const table = [];
  let index = 1;
  while (index < 10 + 1) {
    table.push(number * index);
    index++;
  }
  return table;
}
```

```js
function multiplicationTable(number) {
  const table = [];
  let index = 1;
  while (index < 11) {
    table.push(number * index);
    index++;
  }
  return table;
}
```

```js
function multiplicationTable(number) {
  const table = [];
  let index = 0;
  while (index < 10) {
    table.push(number * (index + 1));
    index++;
  }
  return table;
}
```

```js
function multiplicationTable(number) {
  const table = [];
  let index = 1;
  while (true) {
    table.push(number * index);
    index++;
    if (index > 10) {
      break;
    }
  }
  return table;
}
```

```js
function multiplicationTable(number) {
  const table = [];
  let index = 1;
  while (true) {
    table.push(number * index);
    if (index >= 10) {
      break;
    }
    index++;
  }
  return table;
}
```

```js
function multiplicationTable(number) {
  let index = 1;
  const table = [];
  while (index <= 10) {
    table.push(number * index);
    index++;
  }
  return table;
}
```

```js
function multiplicationTable(number) {
  let index = 1;
  const table = [];
  while (index < 10 + 1) {
    table.push(number * index);
    index++;
  }
  return table;
}
```

```js
function multiplicationTable(number) {
  let index = 1;
  const table = [];
  while (index < 11) {
    table.push(number * index);
    index++;
  }
  return table;
}
```

```js
function multiplicationTable(number) {
  let index = 0;
  const table = [];
  while (index < 10) {
    table.push(number * (index + 1));
    index++;
  }
  return table;
}
```

```js
function multiplicationTable(number) {
  let index = 1;
  const table = [];
  while (true) {
    table.push(number * index);
    index++;
    if (index > 10) {
      break;
    }
  }
  return table;
}
```

```js
function multiplicationTable(number) {
  let index = 1;
  const table = [];
  while (true) {
    table.push(number * index);
    if (index >= 10) {
      break;
    }
    index++;
  }
  return table;
}
```
